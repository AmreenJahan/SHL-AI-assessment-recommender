# SHL Assessment Recommender - Ultra-Lightweight Requirements

# Core Framework (minimal versions)
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation (minimal)
pydantic>=2.0.0,<3.0.0

# HTTP Requests (minimal)
requests>=2.28.0

# Minimal dependencies (no ML libraries!)
python-multipart>=0.0.6

# Total: <50MB installed size
